{
  "name": "optizone-storefront",
  "private": true,
  "version": "2.0.0",
  "description": "OPTIZONE — Vision & Style. Optical storefront: Nuxt 3 (Vue + Tailwind + Inspira UI) frontend, served by a Node/Express + MySQL content API with an admin panel. Try Mirror, booking, EN/עברית/العربية with RTL.",
  "scripts": {
    "dev": "npm --prefix web run dev",
    "dev:server": "node server/index.js",
    "dev:all": "concurrently -n web,api -c cyan,magenta \"npm:dev\" \"npm:dev:server\"",
    "build": "npm --prefix web install --include=dev && npm --prefix web run generate",
    "deploy:build": "npm install --include=dev && npm --prefix web install --include=dev && npm --prefix web run generate",
    "start": "node app.js",
    "seed": "node server/seed-cli.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "express-rate-limit": "^8.5.2",
    "google-translate-api-x": "^10.7.3",
    "helmet": "^8.2.0",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.0.1",
    "mysql2": "^3.11.5",
    "nanoid": "^3.3.7",
    "nodemailer": "^9.0.3"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "playwright": "^1.49.1"
  },
  "engines": {
    "node": ">=20.19"
  }
}
